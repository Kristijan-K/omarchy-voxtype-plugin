-- Keep the preset editor compact; the standard Omarchy presentation terminal
-- is intentionally larger for general-purpose TUI flows.
o.window("org\\.omarchy\\.voxtype-presets", {
  float = true,
  center = true,
  size = { 700, 400 },
})
